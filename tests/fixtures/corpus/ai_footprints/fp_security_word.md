# Security overview

This document discusses the project's security model. It does NOT disable
any security features, set `verify=False`, or open CORS to wildcard. It
just contains the word "security" in narrative text.
