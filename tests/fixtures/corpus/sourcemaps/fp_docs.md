# Source map docs

We explain what a `.map` file is here. We do not include one — just docs.

Use bundlers like webpack or vite to generate source maps for local dev,
and strip them with `--no-source-maps` for production.
